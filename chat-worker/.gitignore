node_modules/
.wrangler/
dist/
.dev.vars
src/corpus-data.ts

# Private content bundled into the Worker but never committed.
# Replace with the real FAQ before deploying; see README.
private/technical-faq.md
